[package]
name = "cnmd-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
regex = "1.10"

[dev-dependencies]
insta = { version = "1.40", features = ["yaml"] }
criterion = "0.5"

[[bench]]
name = "parser_bench"
harness = false
