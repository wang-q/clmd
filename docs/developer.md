# 开发者文档

本文档为 MD 项目开发者提供内部指南，包含测试策略、架构设计、功能计划和开发工作流。

## CommonMark 参考项目分析

### 项目概述

**cmark-0.31.2** 和 **commonmark.js-0.31.2** 是 CommonMark 规范的官方参考实现，分别用 C 语言和 JavaScript 编写。CommonMark 是 Markdown 语法的标准化版本，旨在解决 Markdown 不同实现之间的不一致性问题。

### 核心功能

两个项目都提供了以下核心功能：

1. **解析功能**：将 CommonMark 格式的 Markdown 文本解析为抽象语法树 (AST)
2. **AST 操作**：提供 API 用于操作和遍历 AST
3. **多格式渲染**：将 AST 渲染为 HTML、XML、LaTeX、man 等格式
4. **安全处理**：默认清理原始 HTML 和危险链接，防止 XSS 攻击

### 项目结构

#### cmark-0.31.2 (C 实现)

核心源代码位于 `src/` 目录，主要文件包括：

| 文件 | 功能 |
|------|------|
| cmark.h | 公共 API 头文件，定义节点类型、函数和选项 |
| cmark.c | 实现核心功能，包括解析和渲染 |
| blocks.c | 块级元素解析 |
| inlines.c | 内联元素解析 |
| node.c | 节点操作和树管理 |
| html.c | HTML 渲染器 |
| xml.c | XML 渲染器 |
| latex.c | LaTeX 渲染器 |
| man.c | man 页面渲染器 |

#### commonmark.js-0.31.2 (JavaScript 实现)

核心源代码位于 `lib/` 目录，主要文件包括：

| 文件 | 功能 |
|------|------|
| index.js | 模块导出，定义公共 API |
| node.js | 节点类实现，管理 AST |
| blocks.js | 块级元素解析，包含 Parser 类 |
| inlines.js | 内联元素解析 |
| render/renderer.js | 渲染器基类 |
| render/html.js | HTML 渲染器 |
| render/xml.js | XML 渲染器 |

### 技术架构

#### 1. 解析流程

两个项目都采用类似的解析流程：

1. **词法分析**：将输入文本分解为标记 (tokens)
2. **语法分析**：构建抽象语法树 (AST)
3. **后处理**：处理引用、链接等
4. **渲染**：将 AST 转换为目标格式

#### 2. 节点系统

两者都使用类似的节点系统来表示 AST：

- **块级节点**：document, block_quote, list, item, code_block, html_block, paragraph, heading, thematic_break
- **内联节点**：text, softbreak, linebreak, code, html_inline, emph, strong, link, image

#### 3. API 设计

##### cmark (C API)

提供两种使用方式：
- **简单接口**：`cmark_markdown_to_html` 函数直接将 Markdown 转换为 HTML
- **完整接口**：使用解析器、节点操作和渲染器的完整 API

```c
// 简单接口示例
char *html = cmark_markdown_to_html("Hello *world*", 13, CMARK_OPT_DEFAULT);
free(html);

// 完整接口示例
cmark_parser *parser = cmark_parser_new(CMARK_OPT_DEFAULT);
cmark_parser_feed(parser, "Hello *world*", 13);
cmark_node *root = cmark_parser_finish(parser);
char *html = cmark_render_html(root, CMARK_OPT_DEFAULT);
cmark_node_free(root);
cmark_parser_free(parser);
free(html);
```

##### commonmark.js (JavaScript API)

采用面向对象的设计：
- **Parser 类**：解析 Markdown 文本为 AST
- **Renderer 类**：渲染 AST 为不同格式
- **Node 类**：表示 AST 节点，提供树操作方法

```javascript
// 基本用法
const reader = new commonmark.Parser();
const writer = new commonmark.HtmlRenderer();
const parsed = reader.parse("Hello *world*");
const result = writer.render(parsed);
```

### 技术特点

#### 1. 性能优化

- **cmark**：使用 C 语言实现，性能非常高，声称比原始 Markdown.pl 快 10,000 倍
- **commonmark.js**：性能优秀，与 marked 相当，比其他 JavaScript 实现快很多

#### 2. 标准兼容性

- 两者都严格遵循 CommonMark 规范，通过所有规范测试
- 提供一致的解析结果，确保在不同平台上的一致性

#### 3. 安全性

- 默认清理原始 HTML 和危险链接（如 javascript:、vbscript:、file: 等）
- 可通过选项启用原始 HTML 和危险链接（需谨慎使用）

#### 4. 灵活性

- 支持 AST 操作，可以在解析和渲染之间进行转换
- 提供多种渲染器，支持不同输出格式
- 可扩展，易于添加新的渲染器或修改现有功能

#### 5. 可移植性

- **cmark**：使用标准 C99，无外部依赖，可在多种平台上编译
- **commonmark.js**：纯 JavaScript 实现，可在浏览器和 Node.js 环境中使用

### 关键模块分析

#### 解析器模块

解析器负责将 Markdown 文本转换为 AST，是两个项目的核心部分：

- **块级解析**：处理段落、标题、列表、代码块等
- **内联解析**：处理强调、链接、代码等
- **引用处理**：处理引用和脚注

#### 渲染器模块

渲染器负责将 AST 转换为目标格式：

- **HTML 渲染器**：生成 HTML 输出
- **XML 渲染器**：生成 XML 表示
- **其他渲染器**：cmark 还支持 LaTeX、man 等格式

#### 节点系统

节点系统是 AST 的基础，提供了树结构和操作方法：

- **节点类型**：表示不同类型的 Markdown 元素
- **树操作**：添加、删除、替换节点
- **遍历方法**：提供迭代器用于遍历 AST

### 应用场景

#### cmark 适用场景

- **服务器端渲染**：高性能处理大量 Markdown 内容
- **嵌入式系统**：由于无外部依赖，适合嵌入式环境
- **需要 C 接口的应用**：可作为库集成到 C/C++ 项目中
- **多格式输出**：需要生成 HTML、LaTeX、man 等多种格式的场景

#### commonmark.js 适用场景

- **浏览器端渲染**：客户端实时预览 Markdown
- **Node.js 应用**：服务器端处理 Markdown
- **需要 JavaScript 接口的应用**：可集成到 JavaScript 项目中
- **前端工具**：如编辑器、转换器等

### 技术亮点

1. **高效的解析算法**：特别是 Kārlis Gaņģis 改进的链接和强调解析算法，消除了最坏情况的性能问题
2. **模块化设计**：清晰的职责分离，便于维护和扩展
3. **多格式渲染**：支持多种输出格式，满足不同需求
4. **安全默认值**：默认启用安全模式，防止 XSS 攻击
5. **跨平台兼容性**：可在多种环境中使用

### 参考价值

这两个项目是学习 Markdown 解析和 AST 处理的优秀资源，也是实现 CommonMark 规范的参考标准。对于计划将这些功能转换为 Rust 实现的项目，它们提供了详细的设计参考和实现思路。

- **架构设计**：模块化的设计思路可以直接应用到 Rust 实现中
- **解析算法**：高效的解析算法可以作为 Rust 实现的参考
- **节点系统**：节点类型和树操作的设计可以借鉴
- **API 设计**：可以参考其 API 设计，提供类似的接口
- **测试策略**：可以参考其测试方法，确保与 CommonMark 规范的兼容性

## 开发计划

基于对 CommonMark 参考项目的分析，我们制定以下 Rust 实现的开发计划：

### 第一阶段：基础架构搭建 (已完成)

1. **项目初始化** (已完成)
   - 创建 Rust 项目结构
   - 配置依赖和构建系统
   - 建立基本的模块结构

2. **节点系统实现** (已完成)
   - 定义节点类型（块级和内联）
   - 实现节点数据结构
   - 提供树操作方法（添加、删除、替换节点）
   - 实现节点遍历器

3. **核心 API 设计** (已完成)
   - 设计简单接口（类似 `cmark_markdown_to_html`）
   - 设计完整接口（解析器、渲染器等）
   - 定义配置选项

### 第二阶段：解析器实现 (进行中)

1. **词法分析器**
   - 实现标记 (token) 生成
   - 处理空白、换行等特殊字符

2. **块级解析**
   - 实现段落解析
   - 实现标题解析
   - 实现列表解析
   - 实现代码块解析
   - 实现引用块解析
   - 实现其他块级元素解析

3. **内联解析**
   - 实现强调解析
   - 实现链接解析
   - 实现代码解析
   - 实现其他内联元素解析

4. **引用处理**
   - 实现引用解析和处理
   - 实现脚注处理

### 第三阶段：渲染器实现 (部分完成)

1. **HTML 渲染器** (已完成基础)
   - 实现基本 HTML 渲染
   - 支持安全模式
   - 支持源代码位置信息

2. **XML 渲染器** (已完成基础)
   - 实现 XML 格式渲染

3. **其他渲染器**（可选）
   - 考虑实现 LaTeX 渲染器
   - 考虑实现 CommonMark 渲染器

### 第四阶段：测试和优化

1. **测试策略**
   - 实现单元测试
   - 实现集成测试
   - 使用 CommonMark 规范测试套件
   - 实现性能测试

2. **性能优化**
   - 优化解析算法
   - 优化内存使用
   - 并行处理考虑

3. **安全性**
   - 实现 HTML 清理
   - 实现危险链接检测
   - 进行安全测试

### 第五阶段：文档和发布

1. **API 文档**
   - 编写详细的 API 文档
   - 提供使用示例

2. **用户文档**
   - 编写安装指南
   - 编写使用教程

3. **发布准备**
   - 准备发布版本
   - 发布到 crates.io

### 技术选型

- **语言**：Rust
- **构建工具**：Cargo
- **测试框架**：Rust 标准测试框架
- **依赖**：尽量使用标准库，必要时使用少量第三方库

### 预期目标

- 完全兼容 CommonMark 规范
- 性能接近或超过参考实现
- 提供友好的 API
- 支持多种输出格式
- 安全默认值，防止 XSS 攻击
- 跨平台支持

### 开发工作流

1. **TDD 开发**：先编写测试，再实现功能
2. **模块化开发**：按功能模块分阶段实现
3. **代码审查**：定期进行代码审查
4. **持续集成**：使用 CI 系统自动测试
5. **版本控制**：使用 Git 进行版本管理

### 里程碑

1. **里程碑 1**：基础架构和节点系统完成 (已完成)
2. **里程碑 2**：解析器核心功能完成 (进行中)
3. **里程碑 3**：渲染器核心功能完成 (部分完成)
4. **里程碑 4**：测试覆盖和性能优化完成
5. **里程碑 5**：文档和发布准备完成

## 当前状态

### 已完成的组件

#### 1. 节点系统 (`src/node.rs`)

- 完整的 AST 节点类型定义 (`NodeType`)
- 节点数据结构 (`NodeData`)
- 安全的节点操作函数：
  - `append_child`: 在父节点末尾添加子节点
  - `prepend_child`: 在父节点开头添加子节点
  - `insert_after`: 在指定节点后插入兄弟节点
  - `insert_before`: 在指定节点前插入兄弟节点
  - `unlink`: 从树中移除节点
- 使用 `Rc<RefCell<Node>>` 实现内存安全的树结构
- 节点类型分类方法：`is_block()`, `is_inline()`, `is_leaf()`

#### 2. 迭代器 (`src/iterator.rs`)

- `NodeIterator`: 用于遍历 AST 的迭代器
- `NodeWalker`: 提供 enter/exit 事件的高级遍历器
- `EventType`: 遍历事件类型（Enter, Exit, Done）
- 文本节点合并功能：`consolidate_text_nodes()`

#### 3. 渲染器

- **HTML 渲染器** (`src/render/html.rs`):
  - 支持所有标准节点类型的 HTML 输出
  - HTML 转义功能
  - 安全 URL 检查
  - 源代码位置信息支持

- **XML 渲染器** (`src/render/xml.rs`):
  - 完整的 AST XML 表示
  - 节点属性输出
  - 用于调试和 AST 导出

#### 4. 公共 API (`src/lib.rs`)

- `markdown_to_html()`: 简单的 Markdown 到 HTML 转换
- `parse_document()`: 解析文档返回 AST
- `render_html()`: 将 AST 渲染为 HTML
- `render_xml()`: 将 AST 渲染为 XML
- 配置选项常量：`SOURCEPOS`, `HARDBREAKS`, `UNSAFE`, `NOBREAKS`, `VALIDATE_UTF8`, `SMART`

#### 5. 词法分析器 (`src/lexer.rs`)

- `LineLexer`: 行级别的词法分析器
- 位置追踪：offset, column, line number
- 空白字符处理：支持空格和制表符
- 缩进计算：识别代码缩进（4个空格）
- 辅助函数：`is_space_or_tab()`, `is_blank()`, `split_lines()`

#### 6. 块级解析器 (`src/blocks.rs`)

- `BlockParser`: 块级解析器状态管理
- 逐行处理机制
- 块开始检测：
  - 引用块 (`>`)
  - ATX 标题 (`#`)
  - 围栏代码块 (`` ` `` 或 `~`)
  - 主题分隔线 (`---`, `___`, `***`)
  - 列表项 (`*`, `+`, `-`, `1.`)
- 块级元素创建和管理

### 测试结果

所有 37 个单元测试和 1 个文档测试均通过：
- 节点操作测试
- 迭代器遍历测试
- HTML/XML 渲染测试
- 词法分析器测试
- 块级解析器测试（段落、标题、列表、代码块、引用块、主题分隔线）
- 内联解析器基础测试（行内代码、文本）
- 文档示例测试

### 已完成的块级解析功能

基于对 cmark 和 commonmark.js 参考项目的分析，已完成以下块级解析功能：

1. **容器延续逻辑**：
   - 引用块延续（检测 `>` 标记）
   - 列表项延续（缩进匹配）
   - 围栏代码块延续（围栏匹配）
   - HTML 块延续（类型特定规则）

2. **块开始检测**：
   - 引用块：`>` 开头
   - ATX 标题：`#` 开头（1-6级）
   - Setext 标题：段落后的 `---` 或 `===`
   - 围栏代码块：`` ``` `` 或 `~~~`（至少3个）
   - 缩进代码块：4个空格缩进
   - 主题分隔线：`---`、`___` 或 `***`（至少3个，可混空格）
   - 无序列表：`*`、`+` 或 `-`
   - 有序列表：`1.` 或 `1)`
   - HTML 块：7种类型（`<script`、`<pre`、`<style`、`<!--`、`<?`、`<!`、`<?`）

3. **内容累积**：
   - 段落文本累积
   - 代码块内容处理
   - 列表项内容组织

4. **行级词法分析**：
   - 位置追踪（offset、column、line number）
   - 非空格位置查找
   - 缩进计算
   - 制表符处理

### 已完成的内联解析功能

已实现完整的内联解析器（`src/inlines.rs`）：

1. **核心结构**：
   - `Subject`: 解析状态管理（输入字符串、位置）
   - `Delimiter`: 分隔符栈节点（用于强调/粗体）
   - `Bracket`: 括号栈节点（用于链接/图片）

2. **已实现的内联元素**：
   - 行内代码：反引号分隔，支持多反引号，自动去除首尾空格
   - 硬换行：反斜杠 + 换行
   - 软换行：两个空格 + 换行
   - 转义字符：反斜杠转义
   - 文本节点：普通文本累积，自动合并相邻文本
   - 强调和粗体：`*` 和 `_` 分隔符，支持嵌套
   - 链接：内联链接 `[text](url "title")`，支持尖括号包裹 URL
   - 图片：`![alt](url "title")`，与链接共享解析逻辑
   - **自动链接**：`<url>` 和 `<email>` 自动转换为链接
   - **实体解析**：HTML 实体（`&amp;`、数字实体、十六进制实体）
   - **HTML 内联标签**：开放标签、关闭标签、自闭合标签、注释、处理指令、声明、CDATA
   - 分隔符栈管理：支持强调处理的 opener/closer 匹配

3. **解析流程**：
   - 逐字符扫描
   - 特殊字符检测（`` ` \ & < * _ [ ] ! ``）
   - 分隔符栈管理（用于强调）
   - 括号栈管理（用于链接/图片）
   - 文本节点合并优化

4. **链接解析实现细节**：
   - 方括号匹配：`[` 压栈，`]` 尝试匹配
   - 内联链接目标解析：支持 `<url>` 和普通 URL
   - 链接标题解析：支持 `"`、`'`、`()` 三种引号
   - 图片标记：`![` 作为特殊括号入栈
   - 链接嵌套处理：链接内不能包含其他链接

5. **引用处理器**（`parse_reference`）：
   - 引用定义解析：`[label]: url "title"`
   - 支持无标题的引用定义：`[label]: url`
   - 规范化标签：去除括号、折叠空白、转大写
   - 引用查找：`refmap` 用于存储和查找引用定义
   - 支持三种引用链接形式：
     - 完整引用：`[text][label]`
     - 折叠引用：`[text][]`
     - 简写引用：`[text]`

6. **自动链接实现**：
   - URL 自动链接：`<https://example.com>` → 链接节点
   - Email 自动链接：`<test@example.com>` → mailto: 链接
   - 协议验证：URL 必须以字母开头，后跟字母/数字/+-.
   - Email 验证：必须包含 @ 和域名中的点

7. **实体解析实现**：
   - 命名实体：`&amp;`、`&lt;`、`&gt;`、`&quot;`、`&apos;` 等
   - 十进制数字实体：`&#60;`
   - 十六进制数字实体：`&#x3C;`
   - 无效实体保留原样

8. **HTML 内联标签解析实现**：
   - 开放标签：`<tag>`、`<tag attr="value">`
   - 关闭标签：`</tag>`
   - 自闭合标签：`<tag />`
   - HTML 注释：`<!-- comment -->`
   - 处理指令：`<?xml version="1.0"?>`
   - 声明：`<!DOCTYPE html>`
   - CDATA：`<![CDATA[...]]>`
   - 属性值支持单引号和双引号

### 块级解析器与引用集成

已实现块级解析器与引用定义的集成功能：

1. **引用定义提取**（`finalize_block` 中的段落处理）：
   - 在段落最终化时解析开头的引用定义
   - 支持多个连续的引用定义
   - 更新段落的 source_pos 以反映移除的行数

2. **空段落移除**（`remove_link_reference_definitions`）：
   - 在文档最终化后遍历 AST
   - 识别并移除仅包含引用定义的段落
   - 使用 `__EMPTY_PARAGRAPH__` 标记空段落

3. **引用映射存储**：
   - `refmap` 存储在 `BlockParser` 中
   - 格式：`label -> (url, title)`
   - 标签经过规范化（大写、折叠空白）

### 渲染器增强

已完成渲染器对 `HtmlInline` 节点的支持：

1. **HTML 渲染器** (`src/render/html.rs`)：
   - 支持 `HtmlInline` 节点渲染
   - 安全模式下输出 `<!-- raw HTML omitted -->`
   - 非安全模式下直接输出原始 HTML

2. **XML 渲染器** (`src/render/xml.rs`)：
   - 支持 `HtmlInline` 节点渲染
   - 正确转义 XML 特殊字符

### 完整集成测试

已实现完整的 Markdown 到 HTML 转换流程：

1. **Parser 集成** (`src/parser.rs`)：
   - 整合块级解析器和内联解析器
   - 正确处理段落和标题的内联内容
   - 传递引用映射给内联解析器

2. **新增集成测试** (`src/lib.rs`)：
   - 基础段落转换
   - 标题转换
   - 强调（部分支持）
   - 链接（部分支持）
   - 行内代码
   - 代码块
   - 引用块
   - 无序列表
   - 有序列表
   - 主题分隔线
   - 图片（部分支持）
   - 源代码位置信息

### 测试结果

当前所有 75 个单元测试和 1 个文档测试均通过：
- 节点操作测试
- 迭代器遍历测试
- HTML/XML 渲染测试
- 词法分析器测试
- 块级解析器测试（段落、标题、列表、代码块、引用块、主题分隔线）
- 内联解析器测试：
  - 行内代码
  - 文本节点
  - 强调（基础）
  - 链接（内联）
  - 图片
  - 实体解析
  - 数字实体解析
  - URL 自动链接
  - Email 自动链接
  - 引用定义解析
  - 无标题引用定义
  - 引用链接查找
  - HTML 开放标签
  - HTML 关闭标签
  - HTML 自闭合标签
  - HTML 带属性标签
  - HTML 注释
  - HTML 处理指令
  - HTML 声明
  - HTML CDATA
  - 引用定义移除
  - 多引用定义移除
  - 带标题引用定义
- 集成测试：
  - 基础段落
  - 标题
  - 行内代码
  - 代码块（修复缩进代码块内容捕获）
  - 引用块
  - 列表
  - 主题分隔线
- CommonMark 规范测试套件（基础框架）
- 文档示例测试

### 最近修复

**修复缩进代码块内容捕获问题**：
- 问题：`parser.rs` 中的 `Parser::parse` 方法没有调用 `BlockParser::finalize_document()`
- 影响：代码块内容没有被正确转移到 `literal` 字段，导致 HTML 输出为空
- 修复：在 `parser.rs` 中添加了 `block_parser.finalize_document()` 调用
- 结果：缩进代码块和围栏代码块现在都能正确渲染

**修复 HTML 输出格式以匹配 CommonMark 规范**：
- 问题：HTML 渲染器在输出末尾添加了多余的换行符
- 影响：CommonMark 规范测试失败（0.2% 通过率）
- 修复：在 `render()` 方法末尾去除尾随换行符
- 结果：规范测试通过率提高到 29.3%（183/624 测试通过）

**修复 ATX 标题内容解析问题**：
- 问题：ATX 标题（如 `#\tFoo`）内容为空，以及尾部 `#` 未正确处理（如 `## foo ##`）
- 根因：
  1. `finalize_block` 中没有处理 `NodeType::Heading`，导致标题内容未创建子文本节点
  2. `chop_trailing_hashtags` 函数为空，未实现尾部 `#` 的去除逻辑
- 修复：
  1. 在 `finalize_block` 中添加标题处理，将 `string_content` 转换为子文本节点
  2. 实现 `chop_trailing_hashtags` 函数，正确处理尾部 `#`（包括空格/Tab 和换行符的处理）
- 结果：规范测试通过率从 33.5% 提高到 34.0%（212/624 测试通过）

### 当前状态

- **单元测试**：77 个全部通过
- **文档测试**：1 个通过
- **CommonMark 规范测试**：212/624 通过（34.0%）

### 下一步工作

根据开发计划，接下来需要实现：

1. **完整集成测试**（当前重点）：
   - 修复剩余的 412 个失败的 CommonMark 规范测试用例
   - 主要问题类别：
     - 列表项中的段落处理（松散 vs 紧凑列表）
     - 嵌套列表结构
     - 行内元素解析（强调、链接等）
   - 性能基准测试

2. **渲染器增强**（可选）：
   - HTML 渲染器支持 `HtmlInline` 节点
   - XML 渲染器支持 `HtmlInline` 节点

3. **文档完善**：
   - API 文档
   - 使用示例
   - 性能基准

解析器的实现将继续参考 cmark 和 commonmark.js 的解析算法，采用 TDD 开发策略。
